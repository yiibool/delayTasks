{
    "autoload": {
        "classmap": [
            "./DelayTask"
        ]
    },
    "require": {
        "monolog/monolog": "^1.25",
        "ext-json": "*",
        "ext-curl": "*",
        "ext-bcmath": "*",
        "yiibool/dt-protocol": "dev-master"
    },
    "minimum-stability": "dev",
    "config": {
        "secure-http": true
    }
}
